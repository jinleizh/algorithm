class Solution {
public:
    int titleToNumber(string s) {
        int i = 0;
        int result = 0;
        while(i < s.length()) {
            result = result * 26 + s[i] - 'A' + 1;
            i++;
        }
        
        return result;
    }
};
